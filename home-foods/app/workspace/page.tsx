"use client";

import { useCallback, useEffect, useState, type FormEvent } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import LocationHealth from "@/src/components/location-health";
import DeliveryRouteCard from "@/src/components/delivery-route-card";
import Brand from "@/src/components/brand";
import SavedAddresses from "@/src/components/saved-addresses";
import SellerKitchenEditor from "@/src/components/seller-kitchen-editor";
import WorkspaceMetrics from "@/src/components/workspace-metrics";
import { OriginButton } from "@/src/components/ui/origin-button";
import SpotlightCard from "@/src/components/ui/spotlight-card";

type User = { id: number; email: string; name: string | null; avatarUrl?: string | null; role: "CUSTOMER" | "SELLER" | "RIDER" | "ADMIN" };
type Row = Record<string, unknown>;
const safeRow = (value: unknown): Row => value !== null && typeof value === "object" && !Array.isArray(value) ? value as Row : {};
const money = (value: unknown, currency = "EUR") => new Intl.NumberFormat("fi-FI", { style: "currency", currency }).format(Number(value ?? 0));
const title = (value: unknown) => String(value ?? "").replaceAll("_", " ").toLowerCase().replace(/\b\w/g, (letter) => letter.toUpperCase());

export default function WorkspacePage() {
  const router = useRouter();
  const [user, setUser] = useState<User | null>(null);
  const [data, setData] = useState<Row>({});
  const [addresses, setAddresses] = useState<Row[]>([]);
  const [plans, setPlans] = useState<Row[]>([]);
  const [favoriteRows, setFavoriteRows] = useState<Row[]>([]);
  const [favoriteKitchenRows, setFavoriteKitchenRows] = useState<Row[]>([]);
  const [favoriteTab, setFavoriteTab] = useState<"foods" | "kitchens">("foods");
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");
  const [notice, setNotice] = useState("");
  const [busy, setBusy] = useState(false);
  const [confirmation, setConfirmation] = useState<{ title: string; description: string; label: string; action: () => void } | null>(null);

  const call = useCallback(async (path: string, method = "GET", body?: unknown) => {
    const response = await fetch(path, { method, headers: body ? { "Content-Type": "application/json" } : undefined, body: body ? JSON.stringify(body) : undefined });
    const payload = await response.json();
    if (!response.ok) throw new Error(payload.error ?? "Something went wrong.");
    return payload as Row;
  }, []);

  const load = useCallback(async () => {
    setLoading(true); setError("");
    try {
      const auth = await call("/api/auth");
      const current = auth.user as User | null;
      setUser(current);
      if (!current) { setData({}); return; }
      const endpoint = current.role === "SELLER" ? "/api/seller" : current.role === "RIDER" ? "/api/rider" : current.role === "ADMIN" ? "/api/admin" : "/api/orders";
      let dashboard = await call(endpoint);
      if (current.role === "CUSTOMER") {
        const [addressData, planData, favoriteData] = await Promise.all([call("/api/addresses"), call("/api/subscriptions"), call("/api/favorites")]);
        setAddresses((addressData.addresses ?? []) as Row[]); setPlans((planData.subscriptions ?? []) as Row[]);
        setFavoriteRows((favoriteData.favorites ?? []) as Row[]); setFavoriteKitchenRows((favoriteData.favoriteKitchens ?? []) as Row[]);
        dashboard = { ...dashboard, plans: planData.plans ?? [] };
      }
      setData(dashboard);
    } catch (issue) { setError(issue instanceof Error ? issue.message : "Workspace unavailable."); }
    finally { setLoading(false); }
  }, [call]);
  useEffect(() => { void Promise.resolve().then(load); }, [load]);
  useEffect(() => {
    const isOnlineRider = user?.role === "RIDER" && Boolean((data.rider as Row | undefined)?.isAvailable);
    if (!isOnlineRider) return;
    let stopped = false;
    const heartbeat = async () => {
      if (stopped || document.visibilityState !== "visible") return;
      try {
        const response = await fetch("/api/rider", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ status: "HEARTBEAT" }), cache: "no-store" });
        if (!response.ok) void load();
      } catch { /* API stale timeout will switch this rider offline safely. */ }
    };
    void heartbeat();
    const timer = window.setInterval(() => void heartbeat(), 30_000);
    const onVisible = () => { if (document.visibilityState === "visible") void heartbeat(); };
    document.addEventListener("visibilitychange", onVisible);
    return () => { stopped = true; window.clearInterval(timer); document.removeEventListener("visibilitychange", onVisible); };
  }, [data.rider, load, user?.role]);

  async function perform(action: () => Promise<unknown>, message: string) {
    setBusy(true); setError(""); setNotice("");
    try { await action(); setNotice(message); await load(); }
    catch (issue) { setError(issue instanceof Error ? issue.message : "Couldn't save that change."); }
    finally { setBusy(false); }
  }

  function askConfirmation(action: () => void, titleText: string, description: string, label: string) {
    setConfirmation({ action, title: titleText, description, label });
  }

  async function subscribe(event: FormEvent<HTMLFormElement>, planId: number) {
    event.preventDefault(); const form = new FormData(event.currentTarget);
    setBusy(true); setError("");
    try {
      const result = await call("/api/subscriptions", "POST", { planId, addressId: Number(form.get("addressId")), portions: Number(form.get("portions")), startDate: form.get("startDate"), deliveryTime: form.get("deliveryTime") });
      if (typeof result.checkoutUrl === "string") window.location.assign(result.checkoutUrl);
    } catch (issue) { setError(issue instanceof Error ? issue.message : "Couldn't start billing."); setBusy(false); }
  }
  async function reviewOrder(event: FormEvent<HTMLFormElement>, orderId: number) {
    event.preventDefault(); const form = new FormData(event.currentTarget);
    await perform(() => call("/api/reviews", "POST", { orderId, rating: Number(form.get("rating")), comment: form.get("comment") }), "Thanks for sharing your experience.");
  }
  async function signOut() { await fetch("/api/auth", { method: "DELETE" }); router.push("/"); }
  async function removeFavorite(menuItemId: number) {
    await perform(() => call("/api/favorites", "POST", { menuItemId }), "Removed from your favorites.");
  }
  async function removeKitchenFavorite(shopId: number) {
    await perform(() => call("/api/favorites", "POST", { shopId }), "Kitchen removed from your favorites.");
  }

  if (loading) return <main id="main-content" tabIndex={-1} className="workspace-shell"><div className="workspace-empty"><span className="loading-spinner" aria-hidden="true"/><h1>Setting the table…</h1><p>Just a moment while we bring your workspace together.</p></div></main>;
  if (!user) return <main id="main-content" tabIndex={-1} className="workspace-shell"><Link className="workspace-back" href="/">← HomeFoods</Link><div className="workspace-empty"><span>♡</span><h1>Sign in to your workspace</h1><p>Use the sign-in button on HomeFoods to access orders and account tools.</p><Link className="dark-cta" href="/">Back to the menu <span>→</span></Link></div></main>;

  const rows = (value: unknown) => Array.isArray(value) ? value as Row[] : [];
  const orders = rows(data.orders);
  const assigned = rows(data.assigned);
  const jobs = rows(data.jobs);
  const adminSubscriptions = rows(data.subscriptions);
  const now = new Date();
  const todayOrders = orders.filter((order) => order.createdAt && new Date(String(order.createdAt)).toDateString() === now.toDateString());
  const paidToday = todayOrders.filter((order) => (order.payment as Row | undefined)?.status === "PAID" && (order.payment as Row | undefined)?.currency === "EUR").reduce((sum, order) => sum + Number(order.total ?? 0), 0);
  const pendingOrders = orders.filter((order) => ["PENDING", "CONFIRMED", "PREPARING", "READY_FOR_PICKUP"].includes(String(order.status))).length;
  const deliveredOrders = orders.filter((order) => order.status === "DELIVERED").length;
  const weeklyOrderTrend = Array.from({ length: 7 }, (_, index) => {
    const day = new Date(now); day.setHours(0, 0, 0, 0); day.setDate(day.getDate() - (6 - index));
    const count = orders.filter((order) => order.createdAt && new Date(String(order.createdAt)).toDateString() === day.toDateString()).length;
    return { label: day.toLocaleDateString(undefined, { weekday: "short" }), count };
  });
  const trendMax = Math.max(1, ...weeklyOrderTrend.map((point) => point.count));

  const workspaceLinks = user.role === "CUSTOMER" ? [{ label: "Your orders", href: "#orders", icon: "▤" }, { label: "Meal-plan tracking", href: "/meal-plans", icon: "◷" }, { label: "Favourites", href: "#favorites", icon: "♡" }, { label: "Addresses", href: "#addresses", icon: "⌖" }, { label: "Profile", href: "#profile", icon: "◉" }] : user.role === "SELLER" ? [{ label: "Overview", href: "#seller-dashboard", icon: "◫" }, { label: "Menu & kitchen", href: "#seller-menu", icon: "♨" }, { label: "Orders", href: "#orders", icon: "▤" }, { label: "Profile", href: "#profile", icon: "◉" }] : user.role === "RIDER" ? [{ label: "Available jobs", href: "#rider-dashboard", icon: "⌖" }, { label: "My deliveries", href: "#orders", icon: "➜" }, { label: "Profile", href: "#profile", icon: "◉" }] : [{ label: "Overview", href: "#admin-dashboard", icon: "◫" }, { label: "Kitchens", href: "#kitchens", icon: "♨" }, { label: "Deliveries", href: "#deliveries", icon: "➜" }, { label: "Orders", href: "#orders", icon: "▤" }, { label: "Profile", href: "#profile", icon: "◉" }];
  return <main id="main-content" tabIndex={-1} className={`workspace-shell workspace-${user.role.toLowerCase()}`}><header className="workspace-header"><Brand/><Link className="workspace-back" href="/">← Back to the menu</Link></header><div className="workspace-app-layout"><nav className="workspace-nav" aria-label="Workspace navigation"><div className="workspace-nav-heading"><span className="workspace-nav-mark">HF</span><div><b>{title(user.role)}</b><small>HomeFoods workspace</small></div></div><Link href="/" className="workspace-nav-home"><span aria-hidden="true">⌂</span> Discover HomeFoods</Link><div className="workspace-nav-links">{workspaceLinks.map((link) => <a href={link.href} key={link.href}><span aria-hidden="true">{link.icon}</span>{link.label}</a>)}</div><button type="button" className="workspace-nav-signout" onClick={() => void signOut()}>↪ <span>Sign out</span></button></nav><div className="workspace-main-column"><section className="workspace-title" id="workspace-top"><div><div className="eyebrow"><span className="eyebrow-line"/> {title(user.role)} WORKSPACE</div><h1>Hello, <em>{user.name?.split(" ")[0] || "there"}.</em></h1><p>Everything you need to keep the good food moving.</p></div><span className="workspace-role">{title(user.role)}</span></section><section className="workspace-profile-card" id="profile"><span className="workspace-profile-avatar" role={user.avatarUrl ? "img" : undefined} aria-label={user.avatarUrl ? `${user.name || "User"} profile photo` : undefined} style={user.avatarUrl ? { backgroundImage: `url("${user.avatarUrl}")` } : undefined}>{!user.avatarUrl && (user.name?.trim().split(/\s+/).map((part) => part[0]).slice(0, 2).join("").toUpperCase() || "HF")}</span><div><span className="eyebrow">YOUR PROFILE</span><h2>{user.name || "HomeFoods member"}</h2><p>{user.email} · {title(user.role)}</p><small>Account settings and saved customer addresses are managed in this workspace.</small></div><button type="button" onClick={() => void signOut()}>Sign out</button></section>{error && <p className="workspace-alert" role="alert">{error}</p>}{notice && <p className="workspace-notice">{notice}</p>}

    {user.role === "CUSTOMER" && <><section className="workspace-section" id="orders"><div className="workspace-section-heading"><div><span className="eyebrow">YOUR TABLE</span><h2>Recent orders</h2></div><span>{orders.length} recent</span></div>{orders.length ? <div className="workspace-list">{orders.map((order) => <article className="workspace-card" key={String(order.id)}><div className="workspace-card-top"><div><b>{String((order.shop as Row | undefined)?.name ?? "Home kitchen")}</b><span>{String(order.orderNumber ?? "")}</span></div><span className="status-pill">{title(order.status)}</span></div><p>{rows(order.items).map((item) => `${String(item.quantity)} × ${String(item.name)}`).join(" · ")}</p><div className="workspace-card-bottom"><span>{String((order.delivery as Row | undefined)?.status ? title((order.delivery as Row).status) : "Waiting for cook")}</span><b>{money(order.total, String((order.payment as Row | undefined)?.currency ?? "EUR"))}</b></div>{order.status === "DELIVERED" && !order.review && <form className="review-form" onSubmit={(event) => void reviewOrder(event, Number(order.id))}><label>Rate this order<select name="rating" defaultValue="5"><option value="5">★★★★★ Lovely</option><option value="4">★★★★ Good</option><option value="3">★★★ Okay</option><option value="2">★★ Could improve</option><option value="1">★ Not good</option></select></label><label>Your note<input name="comment" placeholder="How was it?"/></label><button disabled={busy}>Leave review</button></form>}</article>)}</div> : <p className="workspace-muted">Your next homemade meal is waiting. <Link href="/#discover">Explore the menu →</Link></p>}</section>
      <section className="workspace-section" id="favorites"><div className="workspace-section-heading"><div><span className="eyebrow">KEEP THESE CLOSE</span><h2>Your favorites</h2></div></div><div className="favorite-tabs" role="tablist" aria-label="Saved favorites"><button type="button" role="tab" aria-selected={favoriteTab === "foods"} className={favoriteTab === "foods" ? "active" : ""} onClick={() => setFavoriteTab("foods")}>Foods <span>{favoriteRows.length}</span></button><button type="button" role="tab" aria-selected={favoriteTab === "kitchens"} className={favoriteTab === "kitchens" ? "active" : ""} onClick={() => setFavoriteTab("kitchens")}>Kitchens <span>{favoriteKitchenRows.length}</span></button></div>{favoriteTab === "foods" ? favoriteRows.length ? <div className="workspace-list">{favoriteRows.map((favorite) => { const item = favorite.menuItem as Row; const favoriteShop = favorite.shop as Row; return <article className="workspace-card workspace-row favorite-row" key={String(favorite.id)}><Link href={`/kitchens/${String(favorite.shopId)}?item=${String(favorite.menuItemId)}`}><div><b>{String(item?.name ?? "Saved dish")}</b><span>{String(favoriteShop?.name ?? "Home kitchen")} · {money(item?.price)}</span></div><span>View kitchen →</span></Link><button type="button" className="favorite-remove" onClick={() => void removeFavorite(Number(favorite.menuItemId))} aria-label={`Remove ${String(item?.name ?? "dish")} from favorites`} aria-pressed="true">♥</button></article>; })}</div> : <p className="workspace-muted">Your saved dishes will appear here. <Link href="/#discover">Explore homemade favourites →</Link></p> : favoriteKitchenRows.length ? <div className="workspace-list">{favoriteKitchenRows.map((favorite) => { const favoriteShop = favorite.shop as Row; return <article className="workspace-card workspace-row favorite-row" key={String(favorite.id)}><Link href={`/kitchens/${String(favorite.shopId)}`}><div><b>{String(favoriteShop?.name ?? "Home kitchen")}</b><span>{String(favoriteShop?.city ?? "Homemade food nearby")}</span></div><span>View kitchen →</span></Link><button type="button" className="favorite-remove" onClick={() => void removeKitchenFavorite(Number(favorite.shopId))} aria-label={`Remove ${String(favoriteShop?.name ?? "kitchen")} from favorites`} aria-pressed="true">♥</button></article>; })}</div> : <p className="workspace-muted">Your saved kitchens will appear here. <Link href="/#featured">Meet local cooks →</Link></p>}</section>
      <SavedAddresses onChange={() => { void call("/api/addresses").then(r => setAddresses((r.addresses ?? []) as Row[])); }}/>
      <section className="workspace-section"><div className="workspace-section-heading"><div><span className="eyebrow">A LITTLE ROUTINE</span><h2>Your meal plans</h2></div><Link href="/meal-plans" className="meal-tracker-link">Open live tracker →</Link></div>{plans.length ? <div className="workspace-list">{plans.map((subscription) => <article className="workspace-card" key={String(subscription.id)}><div className="workspace-card-top"><div><b>{String((subscription.plan as Row | undefined)?.name ?? "Meal plan")}</b><span>{title((subscription.plan as Row | undefined)?.type)} · {String((subscription.plan as Row | undefined)?.shop && ((subscription.plan as Row).shop as Row).name)}</span></div><span className="status-pill">{title(subscription.status)}</span></div><p>Next meal: {String(subscription.nextDelivery ?? "To be scheduled")}</p><div className="workspace-actions"><button onClick={() => void perform(() => call("/api/subscriptions", "PATCH", { subscriptionId: subscription.id, action: subscription.status === "PAUSED" ? "resume" : "pause" }), subscription.status === "PAUSED" ? "Plan resumed." : "Plan paused.")} disabled={busy}>{subscription.status === "PAUSED" ? "Resume" : "Pause"}</button><button onClick={() => askConfirmation(() => void perform(() => call("/api/subscriptions", "PATCH", { subscriptionId: subscription.id, action: "cancel" }), "Plan cancelled."), "Cancel this meal plan?", "This will stop future subscription renewals.", "Cancel plan")} disabled={busy}>Cancel</button></div></article>)}</div> : <p className="workspace-muted">No meal plans yet. Browse the plans below to make weekday meals easier.</p>}</section><section className="workspace-section"><div className="workspace-section-heading"><div><span className="eyebrow">COOKED FOR YOUR WEEK</span><h2>Meal plans from home cooks</h2></div></div><div className="plan-grid">{rows(data.plans).length ? rows(data.plans).map((plan) => <SpotlightCard className="spotlight-plan" glowColor="purple" size="medium" key={`plan-glow-${String(plan.id)}`}><article className="workspace-card plan-card" key={String(plan.id)}><span className="status-pill">{title(plan.type)}</span><h3>{String(plan.name)}</h3><p>{String(plan.description ?? "Fresh homemade meals from a local kitchen.")}</p><b>{money(plan.price, String(plan.currency ?? "EUR"))} <small>/ {String(plan.type).toLowerCase()}</small></b><form className="workspace-form" onSubmit={(event) => void subscribe(event, Number(plan.id))}><label>Delivery address<select name="addressId" required defaultValue=""><option value="" disabled>Choose a saved address</option>{addresses.map((address) => <option key={String(address.id)} value={String(address.id)}>{String(address.label ?? address.addressLine1)} — {String(address.city)}</option>)}</select></label><label>Start date<input type="date" name="startDate" required/></label><label>Portions<input type="number" min="1" max="20" name="portions" defaultValue="1"/></label><label>Delivery time<input type="time" name="deliveryTime" defaultValue="12:00"/></label><button className="dark-cta" disabled={busy}>Subscribe with Stripe <span>→</span></button></form></article></SpotlightCard>) : <p className="workspace-muted">No active meal plans are published yet.</p>}</div></section></>}

    {user.role === "SELLER" && <><section className="workspace-stats seller-stats" id="seller-dashboard"><div><span>ORDERS TODAY</span><b>{todayOrders.length}</b></div><div><span>PAID SALES TODAY</span><b>{money(paidToday)}</b></div><div><span>NEEDS ATTENTION</span><b>{pendingOrders}</b></div><div><span>DELIVERED ORDERS</span><b>{deliveredOrders}</b></div></section><section className="workspace-chart-card"><div className="workspace-section-heading"><div><span className="eyebrow">REAL ORDER ACTIVITY</span><h2>Orders this week</h2></div><span>Last 7 days</span></div><div className="workspace-bars" role="img" aria-label={`Order counts in the last seven days: ${weeklyOrderTrend.map((point) => `${point.label} ${point.count}`).join(", ")}`}>{weeklyOrderTrend.map((point) => <div className="workspace-bar-column" key={point.label} title={`${point.count} ${point.count === 1 ? "order" : "orders"}`}><b>{point.count}</b><i style={{ height: `${Math.max(5, point.count / trendMax * 100)}%` }}/><small>{point.label}</small></div>)}</div></section><SellerKitchenEditor onSaved={() => void load()}/><section className="workspace-section" id="orders"><div className="workspace-section-heading"><div><span className="eyebrow">TODAY&apos;S SERVICE</span><h2>Orders to prepare</h2></div></div>{orders.length ? <div className="workspace-list">{orders.map((order) => <article className="workspace-card" key={String(order.id)}><div className="workspace-card-top"><div><b>Order {String(order.orderNumber)}</b><span>{rows(order.items).map((item) => `${String(item.quantity)} × ${String(item.name)}`).join(" · ")}</span></div><span className="status-pill">{title(order.status)}</span></div><div className="workspace-card-bottom"><span>{String((order.payment as Row | undefined)?.method ?? "")}</span><b>{money(order.total, String((order.payment as Row | undefined)?.currency ?? "EUR"))}</b></div><div className="workspace-actions">{(["CONFIRMED", "PREPARING", "READY_FOR_PICKUP"] as const).filter((status) => ({ PENDING: "CONFIRMED", CONFIRMED: "PREPARING", PREPARING: "READY_FOR_PICKUP" } as Record<string, string>)[String(order.status)] === status).map((status) => <button key={status} onClick={() => void perform(() => call("/api/seller/orders", "PATCH", { orderId: order.id, status }), `Order marked ${title(status).toLowerCase()}.`)} disabled={busy}>{{CONFIRMED:"Accept order",PREPARING:"Start cooking",READY_FOR_PICKUP:"Ready for collection"}[status]}</button>)}{!["DELIVERED", "CANCELLED", "REFUNDED"].includes(String(order.status)) && <button onClick={() => askConfirmation(() => void perform(() => call("/api/seller/orders", "PATCH", { orderId: order.id, status: "CANCELLED", reason: "Cancelled by seller" }), "Order cancelled."), "Cancel this order?", "The customer will be notified that this order will not be fulfilled.", "Cancel order")} disabled={busy}>Cancel order</button>}</div></article>)}</div> : <p className="workspace-muted">New orders from your neighbours will show up here.</p>}</section></>}

    {user.role === "RIDER" && <><WorkspaceMetrics items={[{label:"AVAILABLE DELIVERIES",value:jobs.length,color:"green"},{label:"ACTIVE ASSIGNMENTS",value:assigned.filter((delivery) => delivery.status !== "DELIVERED").length,color:"orange"},{label:"COMPLETED",value:assigned.filter((delivery) => delivery.status === "DELIVERED").length,color:"purple"}]}/><section className="workspace-section" id="rider-dashboard"><div className="workspace-section-heading"><div><span className="eyebrow">DELIVERY PARTNER</span><h2>Ready when you are</h2></div><label className="availability-toggle"><input type="checkbox" checked={Boolean((data.rider as Row | undefined)?.isAvailable)} onChange={(event) => void perform(() => call("/api/rider", "PATCH", { isAvailable: event.target.checked }), event.target.checked ? "You're online." : "You're offline.")}/><span>{(data.rider as Row | undefined)?.isAvailable ? "Online" : "Go online"}</span></label></div><h3 className="workspace-subhead">Available deliveries</h3>{jobs.length ? <div className="workspace-list">{jobs.map((delivery) => { const order = safeRow(delivery.order); const shopInfo = safeRow(order.shop); const destination = safeRow(order.address); return <article className="workspace-card" key={String(delivery.id)}><div className="workspace-card-top"><div><b>Pickup: {String(shopInfo.name)}</b><span>{String(shopInfo.city ?? "Nearby")}</span></div><span className="status-pill">{title(delivery.status)}</span></div><p>{rows(order.items).map((item) => `${String(item.quantity)} × ${String(item.name)}`).join(" · ")}</p><div className="workspace-card-bottom"><span>Deliver to {String(destination.city ?? "customer")}</span><b>{money(order.deliveryFee, String((order.payment as Row | undefined)?.currency ?? "EUR"))}</b></div><OriginButton onClick={() => void perform(() => call("/api/rider", "PATCH", { deliveryId: delivery.id, status: "ACCEPTED" }), "Delivery accepted.")} disabled={busy || !(data.rider as Row | undefined)?.isAvailable} loading={busy} loadingText="Accepting…">Accept delivery <span>→</span></OriginButton></article>; })}</div> : <p className="workspace-muted">No unassigned deliveries right now. Turn on availability to receive jobs.</p>}</section><section className="workspace-section" id="orders"><div className="workspace-section-heading"><div><span className="eyebrow">ON THE ROAD</span><h2>Your deliveries</h2></div></div>{assigned.length ? <div className="workspace-list">{assigned.map((delivery) => { const order = safeRow(delivery.order); const shopInfo = safeRow(order.shop); const statuses: Record<string,string> = { ASSIGNED: "ACCEPTED", ACCEPTED: "PICKED_UP", PICKED_UP: "IN_TRANSIT", IN_TRANSIT: "DELIVERED" }; const next = statuses[String(delivery.status)]; return <article className="workspace-card" key={String(delivery.id)}><div className="workspace-card-top"><div><b>{String(shopInfo.name)} → {String(safeRow(order.address).city ?? "Address unavailable")}</b><span>Order {String(order.orderNumber)}{order.isSandbox ? " · Sandbox test" : ""}</span></div><span className="status-pill">{title(delivery.status)}</span></div><p><strong>Pickup:</strong> {String(shopInfo.address ?? shopInfo.city ?? "Address unavailable")}</p><p><strong>Deliver to:</strong> {[safeRow(order.address).addressLine1, safeRow(order.address).addressLine2, safeRow(order.address).postalCode, safeRow(order.address).city].filter(Boolean).join(", ")}</p><DeliveryRouteCard shop={shopInfo} address={safeRow(order.address)} notes={order.notes}/><div className="workspace-actions">{next && <OriginButton onClick={() => void perform(() => call("/api/rider", "PATCH", { deliveryId: delivery.id, status: next }), `Delivery marked ${title(next).toLowerCase()}.`)} disabled={busy} loading={busy} loadingText="Updating delivery…">Mark {title(next)} <span>→</span></OriginButton>}{["PICKED_UP", "IN_TRANSIT"].includes(String(delivery.status)) && <><button type="button" disabled={busy} onClick={() => { const reason = window.prompt("Share a short delay update for the customer", "Traffic delay"); if (reason === null) return; void perform(() => call("/api/rider", "PATCH", { deliveryId: delivery.id, status: "DELAYED", reason }), "Delay update shared with the customer."); }}>Report delay</button><button type="button" disabled={busy} onClick={() => { const reason = window.prompt("Tell the customer and support what went wrong", "Delivery could not be completed"); if (reason === null) return; void perform(() => call("/api/rider", "PATCH", { deliveryId: delivery.id, status: "FAILED", reason }), "Delivery issue sent to HomeFoods support."); }}>Report delivery issue</button></>}</div></article>; })}</div> : <p className="workspace-muted">Accepted delivery jobs will appear here.</p>}</section></>}

    {user.role === "ADMIN" && <section className="workspace-section"><div className="workspace-section-heading"><div><span className="eyebrow">FULFILMENT OVERSIGHT</span><h2>Meal-plan schedules</h2></div><span>{adminSubscriptions.reduce((total, subscription) => total + rows(subscription.scheduledMeals).length, 0)} scheduled deliveries</span></div>{adminSubscriptions.length ? <div className="workspace-list">{adminSubscriptions.map((subscription) => { const plan = (subscription.plan as Row | undefined) ?? {}; const shop = (plan.shop as Row | undefined) ?? {}; const scheduled = rows(subscription.scheduledMeals); const deliveredMeals = scheduled.filter((meal) => (meal.order as Row | undefined)?.status === "DELIVERED").length; return <article className="workspace-card" key={String(subscription.id)}><div className="workspace-card-top"><div><b>{String(plan.name ?? "Meal plan")} · {String(shop.name ?? "Kitchen")}</b><span>Plan #{String(subscription.id)} · {title(subscription.status)} · {deliveredMeals}/{scheduled.length} delivered</span></div></div>{scheduled.slice(0, 4).map((meal) => { const order = (meal.order as Row | undefined) ?? {}; const delivery = (order.delivery as Row | undefined) ?? {}; return <div className="admin-scheduled-row" key={String(meal.id)}><span>{new Date(String(meal.scheduledAt)).toLocaleString("fi-FI", { timeZone: "Europe/Helsinki", weekday: "short", day: "numeric", month: "short", hour: "2-digit", minute: "2-digit" })}</span><b>{String(order.orderNumber ?? "Scheduled")}</b><span>{title(delivery.status ?? order.status ?? meal.status)}</span></div>; })}</article>; })}</div> : <p className="workspace-muted">No subscription schedules are recorded yet.</p>}</section>}{user.role === "ADMIN" && <><LocationHealth/><WorkspaceMetrics items={[{label:"SELLERS",value:rows(data.shops).length,color:"green"},{label:"ORDERS",value:orders.length,color:"orange"},{label:"USERS",value:rows(data.users).length,color:"purple"},{label:"PENDING KITCHENS",value:rows(data.shops).filter((shopRow) => shopRow.status === "PENDING").length,color:"blue"},{label:"ONLINE RIDERS",value:rows(data.riders).filter((rider) => rider.isAvailable).length,color:"green"}]}/><section className="workspace-section" id="kitchens"><div className="workspace-section-heading"><div><span className="eyebrow">MARKETPLACE QUALITY</span><h2>Home kitchen applications</h2></div></div><div className="workspace-list">{rows(data.shops).map((shopRow) => <article className="workspace-card workspace-row" key={String(shopRow.id)}><div><b>{String(shopRow.name)}</b><span>{String((shopRow.seller as Row | undefined)?.name ?? "Seller")} · {String(shopRow.city ?? "")}</span></div><span className="status-pill">{title(shopRow.status)}</span><div className="workspace-actions"><OriginButton variant="secondary" onClick={() => void perform(() => call("/api/admin", "PATCH", { action: "shop-status", shopId: shopRow.id, status: "ACTIVE" }), "Shop approved and published.")} disabled={busy} loading={busy} loadingText="Saving…">Approve</OriginButton><button onClick={() => askConfirmation(() => void perform(() => call("/api/admin", "PATCH", { action: "shop-status", shopId: shopRow.id, status: "SUSPENDED" }), "Shop suspended."), "Suspend this kitchen?", "Its menu will no longer be available to customers.", "Suspend kitchen")} disabled={busy}>Suspend</button></div></article>)}</div></section><section className="workspace-section" id="deliveries"><div className="workspace-section-heading"><div><span className="eyebrow">FULFILMENT</span><h2>Unassigned & recovery jobs</h2></div></div>{rows(data.deliveries).length ? <div className="workspace-list">{rows(data.deliveries).map((delivery) => { const order = safeRow(delivery.order); return <article className="workspace-card workspace-row" key={String(delivery.id)}><div><b>{String(safeRow(order.shop).name ?? "Kitchen")}</b><span>Order {String(order.orderNumber)} · {[safeRow(order.address).addressLine1, safeRow(order.address).addressLine2, safeRow(order.address).postalCode, safeRow(order.address).city].filter(Boolean).join(", ") || "Address unavailable"} · Previous rider: {String(((delivery.rider as Row | undefined)?.user as Row | undefined)?.name ?? "None")}</span><span className="status-pill">{title(delivery.status)}</span></div><select aria-label="Choose available rider" value={String(delivery.id)} onChange={(event) => { const riderId = Number(event.target.value); if (riderId) void perform(() => call("/api/admin", "PATCH", { action: "assign-rider", deliveryId: delivery.id, riderId }), "Rider assigned."); }}><option value={String(delivery.id)}>{delivery.status === "FAILED" ? "Choose replacement rider…" : "Assign available rider…"}</option>{rows(data.riders).filter((rider) => rider.isAvailable).map((rider) => <option key={String(rider.id)} value={String(rider.id)}>{String(safeRow(rider.user).name ?? "Rider")}</option>)}</select></article>; })}</div> : <p className="workspace-muted">No delivery jobs are waiting for assignment.</p>}</section><section className="workspace-section" id="orders"><div className="workspace-section-heading"><div><span className="eyebrow">RECENT ACTIVITY</span><h2>Orders</h2></div></div><div className="workspace-list">{orders.map((order) => <article className="workspace-card workspace-row" key={String(order.id)}><div><b>{String(order.orderNumber)} · {String(safeRow(order.shop).name ?? "Kitchen")}</b><span>{String(order.status)} · {money(order.total, String((order.payment as Row | undefined)?.currency ?? "EUR"))}</span></div><span>{title((order.payment as Row | undefined)?.status)} · {title((order.delivery as Row | undefined)?.status)} · Rider: {String((((order.delivery as Row | undefined)?.rider as Row | undefined)?.user as Row | undefined)?.name ?? "Unassigned")}</span>{(order.payment as Row | undefined)?.status === "PAID" && <button onClick={() => askConfirmation(() => void perform(() => call("/api/admin", "PATCH", { action: "refund-order", orderId: order.id }), "Stripe refund started."), "Refund this payment?", "A full refund will be sent to the customer through Stripe.", "Issue refund")} disabled={busy}>Refund</button>}</article>)}</div></section></>}

      </div></div>
    {confirmation && <div className="modal-backdrop" role="presentation" onClick={() => setConfirmation(null)}><section className="confirm-modal" role="alertdialog" aria-modal="true" aria-labelledby="confirm-title" aria-describedby="confirm-description" onClick={(event) => event.stopPropagation()}><span className="eyebrow">PLEASE CONFIRM</span><h2 id="confirm-title">{confirmation.title}</h2><p id="confirm-description">{confirmation.description}</p><div className="confirm-actions"><button type="button" onClick={() => setConfirmation(null)}>Keep it</button><button type="button" className="confirm-danger" onClick={() => { const action = confirmation.action; setConfirmation(null); action(); }}>{confirmation.label}</button></div></section></div>}
    <footer className="workspace-footer"><Brand compact/><span>{user.email}</span></footer></main>;
}
