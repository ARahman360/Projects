import './load-next-env';import {db} from '../src/prisma/db';const s=await db.orm.public.Shop.where({id:2}).include('seller').first();if(s)console.log(JSON.stringify({shopId:s.id,name:s.name,status:s.status,seller:s.seller?{id:s.seller.id,name:s.seller.name,email:s.seller.email,role:s.seller.role,passwordConfigured:Boolean(s.seller.password),passwordFormat:s.seller.password?.split("$")[0]}:null}));await db.close();
